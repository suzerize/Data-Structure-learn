## 链表定义
```cpp
typedef  struct Lnode
{
	int data;
	Lnode* next;
} Lnode ,*LinkList;
```

## 初始化链表
使用指针：先分配空间，才能用
```cpp
void  InitLinkList(LinkList&l)
 {
	l = new Lnode;
	l->next = NULL;
 }
 ```

## 创建链表
造出一个完整的有数据的链表
```cpp
void FoundList_B__0206(LinkList__0206&l)
{
 Lnode__0206* p = l;
 int n;
 cout << "请输入元素个数：" << endl;
 cin >> n;
 cout << "请依次输入元素：" << endl;
 for (int i = 0; i < n; i++)
 {
	 int e;
	 cin >> e;
	 Lnode__0206* newLnode = new Lnode__0206;
	 newLnode->data = e;
	 newLnode->next =NULL;
	 p->next = newLnode;
	 p = p->next;
 }
}
```
 ## 求表长
 注意到这里的迭代器初始化为l.next，计数器初始化为0
 原因是计数器作为看数了多少个结点了，与后面的3个函数不同
 ```cpp
 int LinkListLength__0206(LinkList__0206& l)
{
	Lnode__0206* p = l->next;
	int j = 0;
	while (p)
	{
		j++;
		p = p->next;
	}

	return j;
}
```

## 找到元素e在第几位
迭代器初始化为l.next，计数器初始化为1，同步（也可以p=l,j=0）
```cpp
bool LocateElem__0206(LinkList__0206&l,int e,int &i)
{
 Lnode__0206* p = l->next;
 int j = 1;
 //Lnode__0206* p = l->next;
 while (p)
 {
	 if (p->data == e)
	 {
		 i = j;
		 return true;
	 }
	 p = p->next;
	 j++;
 }
 return false;
}
```

## 获取第i个元素
迭代器初始化为l.next，计数器初始化为1，同步（不可以将p初始为l，j为0，因为要用到数据域，头结点与其他结点产生了不同）
```cpp
bool GetElem__0206(LinkList__0206&l,int i, int& e)
{
 Lnode__0206* p = l->next;
 int j = 1;
 while (p && j < i )
 {
	 p = p->next;
	 j++;
 }
 if (!p || j > i )
 {
	 return false;
 }
 e = p->data;
 return true;

}
```

## 在第i个位置插入
迭代器初始化为l，计数器初始化为0，同步
```cpp
 bool InsertList__0206(LinkList__0206&l,int i, int e)
 {
	 Lnode__0206* p = l;
	 int j = 0;

	 while (p || j < i - 1)
	 {
		 j++;
		 p = p->next;
	 }
	 if (!p || j > i - 1)
	 {
		 return false;
	 }

	 Lnode__0206* newLnode = new Lnode__0206;
	 newLnode->data = e;
	 newLnode->next = p->next;
	 p->next = newLnode;
	 return true;
 }
 ```

 ## 删除第i个结点
 迭代器初始化为l，计数器初始化为0，同步
 ```cpp
 bool DeleteList__0206(LinkList__0206& l, int i)
{
 //初始化迭代器与计数器
 Lnode__0206* p = l;
 int j = 0;
 
 //遍历找到删除结点的前一个结点
 while (p->next && j < i - 1)
 {
	 p = p->next;
	 j++;
 }

 //判断i是否越界违法
 if (!p->next || j > i - 1)
 {
	 return false;
 }

 //先取出结点，再进行删除
 Lnode__0206* oldone = p->next;
 p->next = (p->next)->next;
 delete oldone;
 return true;
}
```

对比如上五个函数，发现除了求长函数，其余都是迭代器与计数器同步，只有它是迭代器快于计数器，记录走过了几个结点  
剩余函数中，迭代器记录当前到哪个结点了

得出的结论是：迭代器看最后停留的位置是第i个还是第i-1个。  
如果是i-1，则天然少1，  
如果是i，则可以初始化为首元结点，若头结点的数据域没有用到（那和普通结点一样了）就可以初始化为首元结点，只会多数一次，不会出错   
