# 重点
理解何时需要加入模运算以达到循环的目的
理解如何区分队空与队满
重点看的是入队出队与求长三个方法

## 定义抽象类
队列要操作的是头**和**尾，需要移动，仍然要用指针，但是由于要形成循环（整形的话使用模运算就会很简单），  
所以指针是记录实际的下标而不是真的指针类型
```cpp
struct Queue__030502
{
	int* data;
	int front;
	int rear;
};
```

## 初始化
顺序实现第一步就是确定大小，分配空间，指针置空，这里就赋值0
```cpp
Queue__030502()
{
	data = new int[MAXSIZE];
	if (!data)
	{
		cout << "分配空间失败" << endl;
		exit(OVERFLOW);
	}

	front = rear = 0;
}
```

## 入队与出队
一起讲，因为重点在这里。  
1. 一般情况下：入队入满，然后出队出空，边界就是入队：队满，出队：队空  
   如何判断队满：那就是尾指针到头了，此时尾指针和头指针分在队两头。  
   如何判断队空：头==尾  
2. 特殊情况：入队不满，然后出队，然后再入队，会发现有空间但是尾指针已经到头了，入不了队了，这叫做假溢出  
   原因：出队时现实的队伍会往前走，但顺序队不太可能想让元素全体前移（时间复杂度高，要这么做不如用链队了）  
   解决方法：使顺序队形成前进的效果，通过循环，循环的好处是可以重复利用同一片空间（只要队没满），使得尾指针在到头之后还可以接着起始位置走
   实现：取余， **让可能出现指针移动的地方都模上长度** ，这样就实现循环了
3. 新的问题：由于尾后即头，那么我入队入满了之后尾到哪去了？没错，头，也就是尾==头，其余判空条件相同了
   原因：这个环的所有结都是相同的，没有特殊的位置以供判断队满
   解决： **空一个位置** ，这样队满条件就会变成尾+1==头，产生了差异，并且这个空的位置不固定，其应该是头的上一个位置，我画图验证过
4. 总结：要将每一个指针移动的操作加上模运算（只要在顺序队里），模运算的对象要加一以形成空一位的效果
```cpp
//入队
bool Push__030502(int& e)
{
	//队满
	//经过画图验证，空的那个结点并非不变的
	if ((rear + 1)%MAXSIZE == front)
	{
		cout << "队已满" << endl;
		return false;
	}
	data[rear] = e;
	//使其加一，并且形成循环
	rear = (rear + 1) % MAXSIZE;
	//cout << "rear：" << rear << endl;
	return true;
}
```
```cpp
//出队
bool Pop__030502()
{
	//队空
	if (rear == front)
	{
		cout << "队空无法出队" << endl;
		return false;
	}

	//使其加一，并且形成循环
	front = (front + 1) % MAXSIZE;
	return true;
}
```
## 求长
对于一切可能让指针移动的都要加模，这里为什么？  
还是因为循环，尾可能小于头，所以考虑的是特殊情况
```cpp
//求长
int length__030502()
{
	return (rear - front + MAXSIZE) % MAXSIZE;
}
```
