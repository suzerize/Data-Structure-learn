我只理解一点原理，但是不理解代码，等我再学一些东西回来补充代码部分


## 原理
通过跳过不可能完全匹配的部分，简化比较，后续优化也是这样优化的  

## 前置知识
前缀：一段字符串从开头到尾巴的前一位，任意长度都是前缀  
后缀：一段字符串从尾巴到开头的前一位，任意长度都是后缀  
例：abcd  
前缀：a ab abc  
后缀：d cd bcd  

# next原理
1. 比较字符串遇到失配情况，主串不回溯，子串回溯到的位置记录在next数组中   
2. 失配时，在已配对的部分若存在最长的一段前缀与后缀相等，下次比较时i不变，j回溯到最长的前缀的后面，  
3. 我可以直接跳到前缀之后，因为我跳过了一定比不上的，我有最长相等前缀，那么其他前缀都会比它短或者可能压根不匹配，比较这些浪费了时间  

## 省时间之处
1. 若BF，则失配后回到i-j+2的位置，也就是刚才比较的下一个位置，j=1  
2. 但KMP，则直接找到BF将查找的区间中最长的最好的位置，到那里开始重新比较，（因为比较不会达到子串的长度，因此刚刚比较的这一部分必然最多只是子串的一部分，我要减少重复查找那便要最长的那一部分）  
3. 省掉了【查找到相对短的部分并且进行比较】和【不匹配迭代】两部分时间  

## nextval原理
1. 若子串中出现重复元素，**并且**它们的next表格有跳转关系，比如从一个'b'跳到前面的一个'b'，但此时i所指元素已经和'b'失配了，这时再和前面的'b'比，位置变了，结果不可能变，是一定错误的比较  
2. nextval通过优化next的跳转进行优化，失配时它将比较next[j]记录位置的元素，若跳转到相同元素，那么这次跳转无意义，应跳转到next[next[j]]，也就是跳转到的地方它要跳转的地方，也就是将要跳转两次的操作折叠成一次  
3. 然而，我的nextval是从前向后记录的，后面的记录是根据前面，前面若是发生一次折叠，后面再用到它会发生两次折叠，因为我已经处理过的都是一步到最简的，后面仿照前面，也是最简的，省的时间就在这折叠的部分  

## 手算？
1. next：长度与子串一致，对应元素为这一位之前（不包括这一位）最长的相等前后缀  
2. nextval：在next的基础上，第一位0，第二位开始若next[j]==t[j]，也就是失配元素与失配后跳转的元素一样了，则nextval[j]=next[next[j]]，放入失配跳转的跳转
不相等则放入next即可

有一个例子
主串：a b a b a b c
子串：a b a b c
