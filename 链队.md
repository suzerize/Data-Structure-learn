## 简要感想
链队的实现与链表的实现高度类似，也就是说链表是基础，基础不牢地动山摇


### 出过的错
1. 未判断front是否为空就访问其数据域，导致报错 --访问指针要记得它一定先要有东西可指 --> 习惯性错误
2. 判断front为空后不返回，仍让其访问其访问不了的东西 --和上一个是配套的，记得指针要是没东西可指还需要继续吗 --> 习惯性错误
3. 以为空链队与已销毁链队的条件是一个 --> 知识性错误
4. exit函数时结束程序运行并抛出错误代码 -->知识性错误

### 定义抽象类
链队也由结点与头尾指针构成，

```cpp
//结点抽象类定义
typedef struct Qlnode
{
	int data;
	Qlnode* next;

}Qlnode;

//链队抽象类定义
struct QLink
{
	//一个链队有头指针和尾指针
	Qlnode* front;
	Qlnode* rear;
  //……成员方法
}
```
在这个地方迷惑过：  
我该怎么在结点的定义里加上两个指针呢，其实在链表的结点定义内部也没有定义两个指针，其只是用typedef定义了能指向结点的指针LinkList，将其视为头指针。    
所以正确的做法就是定义一个新的链队类，包含头尾指针。  
不应该typedef两个同类型的指针一个当头指针一个当尾指针，因为头尾指针应该同时出现，属于一个链队，而不是由我记得谁的头尾指针叫什么。    

举个例子
```cpp
//错误的定义方法
typedef struct Qlnode
{
	int data;
	Qlnode* next;

}Qlnode,*rear,*front;

void test02()
{
	rear r3;
	rear r2;
	rear r3;
	front f1;
	front f2;
	front f3;
	QLink q1;
	q1.front;
	q1.rear;
}
```
如果我要typedef头尾指针，那么我要知道r1对应f1，如果打包定义，它们同属于一个对象，不可能找错。  

### 初始化
让头尾指针都指向头结点即可
```cpp
QLink()
{
	front = new Qlnode;
	if (!front)
	{
		exit(OVERFLOW);
	}
	front->next = NULL;
	rear = front;
}
```
### 创建一个完整链队
链队的创建可以全权由入队实现，这个方法是多余的，不过我想到了要更新指针还挺好
```cpp
//创建一个链队
void FoundQLink()
{
	int n;
	cout << "输入链队大小：" << endl;
	cin >> n;
	cout << "输入元素" << endl;
	while (n--)
	{
		Qlnode* newLnode = new Qlnode;
		if (!newLnode)
		{
			exit(OVERFLOW);
		}
		cin>>newLnode->data;
		rear->next = newLnode;
		newLnode->next = NULL;
		//莫忘记尾指针更新
		rear = rear->next;
	}
}
```

### 销毁
记得指针用不上了要置空
```cpp
//销毁
void DeleteQL()
{
	while(front)
	{
		rear = front->next;
		delete front;
		front = rear;


	}
	//别忘记置空，否则if用不了
	front = rear = NULL;
}
```

### 打印全部元素
注意到空链队与已销毁链队的差别：空链队是头==尾，已销毁链队是头==尾==NULL
```cpp
//打印全部元素
void print()
{
	//因为front不存在，访问front.next也是非法的
	if (!front)
	{
		cout << "链队已销毁" << endl;
		//因为非法所以下面的操作都做不了，需要跳过
		return;
	}
	//链队为空和已销毁不一样
	if (front == rear)
	{
		cout << "链队为空" << endl;
		return;
	}
	Qlnode* p = front->next;

	while (p )
	{
		cout << p->data << " ";
		p = p->next;
	}
	cout << endl;
}
```

### 入队
很简单
```cpp
//入队，尾插法，因为链式所以基本不会上溢
void QL_Push_back(int e)
{
	Qlnode* newLnode = new Qlnode;
	if (!newLnode)
	{
		exit(OVERFLOW);
	}
	newLnode->data = e;
	newLnode->next = NULL;
	rear->next = newLnode;
	rear = rear->next;
}
```

### 出队
注意，链队不会上溢，但仍可能下溢，判断边界  
存在两个边界：  
1. 如果链队为空则不出队
2. 如果出队的元素是最后一个，rear在出队后野了，要赋值为front
```cpp
//出队
bool QL_Pop_head()
{
	if (front == rear)
	{
		cout << "链队为空，出队失败" << endl;
		//抛出错误就结束运行了
		exit(UNDERFLOW);
	}
	Qlnode* deleteone = front->next;
	front->next = deleteone->next;
	delete deleteone;
	if (!front->next)
	{
		rear = front;
	}
	return true;
}
```
