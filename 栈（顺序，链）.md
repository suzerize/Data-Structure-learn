# 感受
栈的实现并不难，但是栈与递归相关，使得理解与使用更困难一些，可以从做题中学习更多，不必老看这基本用法

## 顺序栈

### 定义顺序栈抽象类
一个栈，要操作的就是栈顶，需要头尾指针，以判断是否到底，因为是顺序的，所以有最大容量

```cpp
struct SqStack
{
	int* top;
	int* base;
	int stacksize;
};
```

### 初始化栈
还是因为是顺序的，所以步骤是知道大小，然后按大小分配空间，然后将头尾指针都指向这空间即可
```cpp
//初始化栈，写在类内，就不用手动写了
SqStack(int n)
{
	stacksize = n;
	base = new int[stacksize];
	if (!base)
	{
		cerr << "初始化失败" << endl;
		exit(OVERFLOW);
	}
	top = base;
}
```

### 判空函数
已销毁的顺序栈头尾指针相等并且等于空，而空栈头尾指针相等并且不为空（指向初始化分配的空间）
```cpp
//判空
bool SqStackisEmpty(SqStack& s)
{
	if (s.base == s.top)
		return true;
	return false;
}
```

### 求长
栈的头指针指向最后一个元素的下一个位置（即下标为size的位置），尾指针指向栈底（下标为0），故而相减即为size-0=size
```cpp
//求长
int SqStackLength(SqStack& s)
{
	
		return s.top - s.base;
}
```

### 清空
由空栈的头尾相等逆推清空就是让头尾相等，未处理的元素会被覆盖掉（如果用到的话）
```cpp
//清空
void SqStackClear(SqStack& s)
{
	s.top = s.base;
}
```

### 销毁
销毁即释放一个栈所有空间，并将指针还原为空
```cpp
//销毁
void SqStackDestroy(SqStack& s)
{
	if (s.base)
	{
		//头尾指针共享空间，不用重复释放
		delete[] s.base;
		s.stacksize = 0;
		s.top = s.base = NULL;
	}

}
```

### 入栈
入栈的边界是栈满，判断栈满看长度
实现入栈画个图，总之就是非常简单
```cpp
//入栈
bool SqStackPush(SqStack& s,int e)
{
	if (s.top - s.base == s.stacksize)
	{
		cout << "上溢" << endl;
		return false;
	}
	//*s.top = e;
	//s.top++;
	*s.top++ = e;
	return true;
}
```

### 出栈
出栈的边界是栈空，判断也看长度
头指针指的是最后一个元素下一个地方，所以先让他指向要出栈的元素，再把其所指返回就是最后一个元素
```cpp
bool SqStackPop(SqStack& s, int& e)
{
	if (s.top - s.base == 0)
	//可换s.top==s.base
	{
		cout << "下溢" << endl;
		return false;
	}
	
	//s.top--;
	//e = *s.top;
	e = *--s.top;
	return true;
}
```

### 小结
顺序栈与顺序表很像，所以其操作重点在于判断长度而不是应用指针


## 链栈
链栈与链表的操作也是高度相似的，但是其操作都是对于最后的元素，所以指针域里存的应该是上一个元素的地址

### 定义链栈抽象类
与链表完全一样，但这里next指的是栈顶到栈底，对应到链表就是从后指向前，存的是前一个元素地址
```cpp
typedef struct StackLnode
{
	int data;
	StackLnode* next;
}StackLnode,*StackLink;
```

### 初始化

```cpp
//初始化，因为没有头结点，所以连空间都不用分配
void InitStackLink__030303(StackLink__030303& s)
{
	s = NULL;
}
```

### 判空
空链栈的头指针置空
```cpp
//判空函数
bool IsEmpty_StackLink__030303(StackLink__030303& s)
{
	return s == NULL;
}
```

### 入栈
创建新结点，其存储上一个元素地址，然后让头指针指向他
```cpp
//入栈
void StackLink_push_back__030303(StackLink__030303& s, int e)
{
	StackLnode__030303* p = new StackLnode__030303;
	p->data = e;
	p->next = s;
	s = p;

}
```

### 打印
不用看，没创新点
```cpp
//输出所有数据
void printStack__030303(StackLink__030303& s)
{
	StackLnode__030303* p = s;
	if (IsEmpty_StackLink__030303(p))
	{
		cout << "链栈为空" << endl;
		return;
	}
	while (p)
	{
		cout << p->data << " ";
		p = p->next;
	}
	cout << endl;
}
```

### 出栈
对于链式，依旧没有上溢，但有下溢
```cpp
bool StackLink_pop_back__030303(StackLink__030303& s)
{
	if (s)
	{
		StackLnode__030303* p = s;
		s = s->next;
		delete p;
		return true;
	}
	cout << "链表为空，无法出栈" << endl;
	return false;
}
```

### 获取栈顶元素
头指针指向的就是栈顶，只要它不为空就行
```cpp
//获取栈顶元素
bool GetElem_StackLink__030303(StackLink__030303& s, int& e)
{
	if (s)
	{
		e = s->data;
		return true;
	}
	return false;
}
```

### 小结
比顺序栈还要简单，就是将链表掉个个儿注意一下，其他没什么变化
